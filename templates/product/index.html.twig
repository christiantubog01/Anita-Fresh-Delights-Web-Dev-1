{% extends 'dashboard_base.html.twig' %}

{% block title %}Product index{% endblock %}

{% block body %}

    <h1>Products</h1>

    <table id="myTable" class="display nowrap" style="width:100%">
    <thead>
        <tr>
            <th>ID</th>
            <th>Product Name</th>
            <th>Product Description</th>
            <th>Image</th>
            <th>Price</th>
            <th>Category</th>
            <th>Stock Quantity</th>
            <th>Date Created</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        {% for product in products %}
            <tr>
                <td>{{ product.id }}</td>
                <td>{{ product.productName }}</td>
                <td>{{ product.productDescription }}</td>
                <td>
                {% if product.image %}
                                <img src="{{ asset('products/' ~ product.image) }}" alt="{{ product.ProductName }}" class="product-thumb">
                            {% else %}
                                <span class="no-image">No image</span>
                            {% endif %}
                </td>
                <td>{{ product.price }}</td>

                {# Category relation (change id to categoryName if you want to display category name)#}
                <td>{{ product.category ? product.category.id : '' }}</td> 
                

                {# Stock relation (assuming OneToOne or ManyToOne) #}
                <td>{{ product.stock ? product.stock.quantity ~ ' ' ~ product.stock.unit : '' }}</td>

                <td>{{ product.dateCreated ? product.dateCreated|date('Y-m-d') : '' }}</td>

                <td>
                    <a href="{{ path('app_product_show', {'id': product.id}) }}" class="btn btn-sm btn-primary">➤ Show</a> 
                    <a href="{{ path('app_product_edit', {'id': product.id}) }}" class="btn btn-sm btn-secondary">✎ Edit</a>
                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>

    <a href="{{ path('app_product_new') }}">Create new</a>
{% endblock %}
