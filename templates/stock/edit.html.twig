{% extends 'dashboard_base.html.twig' %}

{% block title %}Edit Stock{% endblock %}

{% block body %}
<h1 class="head_title">EDIT STOCK</h1>

<div class="edit-card">

    {{ include('stock/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="actions">
        <a class="btn btn-secondary" href="{{ path('app_stock_index') }}">🡰 Back</a>
        {{ include('stock/_delete_form.html.twig') }}
    </div>

</div>
{% endblock %}
