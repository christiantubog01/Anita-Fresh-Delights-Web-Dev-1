{% extends 'dashboard_base.html.twig' %}

{% block title %}New Stock{% endblock %}

{% block body %}
    <h1>Create new Stock</h1>

<div class="edit-card">

    {{ include('stock/_form.html.twig', { button_label: 'Create' }) }}

    <div class="actions">
    <a href="{{ path('app_stock_index') }}" class="btn btn-secondary">🡰 Back</a>
    </div>
</div>
{% endblock %}
