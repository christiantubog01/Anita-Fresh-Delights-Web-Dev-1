{% extends 'dashboard_base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
    <h1>Create new User</h1>
<div class="edit-card">
    {{ include('user/_form.html.twig', { button_label: 'Create User' }) }}

    <div class="actions">
    <a class="btn btn-secondary" href="{{ path('app_user_index') }}">🡰 Back</a>
    </div>
</div>
{% endblock %}
