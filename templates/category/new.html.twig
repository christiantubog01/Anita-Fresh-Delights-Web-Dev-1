{% extends 'dashboard_base.html.twig' %}

{% block title %}New Category{% endblock %}

{% block body %}
    <h1 class="head_title">CREATE CATEGORY</h1>

    <div class="edit-card">

    {{ include('category/_form.html.twig') }}

    <div class="actions">
    <a href="{{ path('app_category_index') }}" class="btn btn-secondary">🡰 Back</a>
    </div>
    </div>
{% endblock %}
